name: CI

on:
  - push

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v6
      
      - name: build
        run: |
          make build
      
      - name: tests
        run: |
          make test
